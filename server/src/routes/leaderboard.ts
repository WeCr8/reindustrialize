/**
 * GET  /leaderboard?scope=global|org — Arcade global; Shop Mode per-org (opt-in)
 * POST /leaderboard                  — only from validated saves (see saves.ts)
 * Org entries anonymized by default ("A San Diego job shop") until org admin opts in.
 */
export {};
