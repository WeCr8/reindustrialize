/**
 * SHOP MODE — auth-gated embed for jobline.ai.
 * Requires JobLine session; linkShop() exchanges it for an org-scoped game JWT.
 * Polls /bridge/events; real shop events grant in-game rewards (LIVE badges,
 * Communication skill points, org leaderboard).
 */
export {};
